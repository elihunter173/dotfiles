# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# AS OF i3blocks v1.4 envars in blocks are not supported

# Global properties
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
# Change $SCRIPT_DIR to the location of your scripts!
command=~/.config/i3blocks/contrib/$BLOCK_NAME/$BLOCK_NAME
separator_block_width=12
markup=none

# This displays "ARTIST - SONG" if a music is playing.
# Supported players are: spotify, vlc, audacious, xmms2, mplayer, and others.
[mediaplayer]
instance=spotify
interval=5
signal=10

# Volume indicator
# See the script for details.
[volume]
label=♪
label=VOL
interval=once
signal=10

# Memory usage
# The type defaults to "mem" if the instance is not specified.
# [memory]
# label=MEM
# interval=30
# separator=false
# separator_block_width=8
# [memory]
# label=SWAP
# instance=swap
# interval=30

# CPU usage
# The script may be called with -w and -c switches to specify thresholds,
# see the script for details.
# [cpu_usage]
# label=CPU
# interval=30
# min_width=CPU 100.00%

# Network monitoring
# [iface]
# interval=30
# separator=false
# [bandwidth2]
# interval=persist
# markup=pango
# [wifi]
# INTERFACE=wlp4s0
# label=wifi:
# interval=10

# Since i3blocks v1.4 doesn't support envars in blocks, I have to use the
# script which is gross
# Battery indicator
[battery]
command=BAT_NUMBER=0 ~/.config/i3blocks/contrib/battery/battery
label=BAT0
interval=30
# BAT_NUMBER=0
[battery]
command=BAT_NUMBER=1 ~/.config/i3blocks/contrib/battery/battery
label=BAT1
interval=30
# BAT_NUMBER=1

# Date Time
[time]
command=date '+%Y-%m-%d %H:%M'
interval=10

# Generic media player support
