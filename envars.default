# vim: filetype=sh

# A collection of required config for each machine I set up
#
# Required Variables:
#     EDITOR: The text editor used for this machine. (vim or nvim normally)
#     TERM_EMU: The terminal emulator used for this machine.

export EDITOR=
export TERM_EMU=
